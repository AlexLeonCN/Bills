<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.alex.bills.mapper.LedgerMapper">

    <resultMap id="LedgerResultMap" type="org.alex.bills.model.Ledger">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="desc" property="desc"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <insert id="insert" parameterType="org.alex.bills.model.Ledger">
        insert into ledger (id, name, desc, create_time, update_time)
        values (#{id}, #{name}, #{desc}, #{createTime}, #{updateTime})
    </insert>

    <update id="update" parameterType="org.alex.bills.model.Ledger">
        update ledger
        set name = #{name},
            desc = #{desc},
            create_time = #{createTime},
            update_time = #{updateTime}
        where id = #{id}
    </update>

    <delete id="deleteById">
        delete from ledger where id = #{id}
    </delete>

    <select id="findById" resultMap="LedgerResultMap">
        select id, name, desc, create_time, update_time
        from ledger
        where id = #{id}
    </select>

    <select id="findAll" resultMap="LedgerResultMap">
        select id, name, desc, create_time, update_time
        from ledger
    </select>
</mapper>
