<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.alex.bills.mapper.ProjectMapper">

    <resultMap id="ProjectResultMap" type="org.alex.bills.model.Project">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="desc" property="desc"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <insert id="insert" parameterType="org.alex.bills.model.Project">
        insert into project (id, name, desc, create_time, update_time)
        values (#{id}, #{name}, #{desc}, #{createTime}, #{updateTime})
    </insert>

    <update id="update" parameterType="org.alex.bills.model.Project">
        update project
        set name = #{name},
            desc = #{desc},
            create_time = #{createTime},
            update_time = #{updateTime}
        where id = #{id}
    </update>

    <delete id="deleteById">
        delete from project where id = #{id}
    </delete>

    <select id="findById" resultMap="ProjectResultMap">
        select id, name, desc, create_time, update_time
        from project
        where id = #{id}
    </select>

    <select id="findAll" resultMap="ProjectResultMap">
        select id, name, desc, create_time, update_time
        from project
    </select>
</mapper>
